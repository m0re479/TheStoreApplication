spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ForTestSequrity
    username: postgres
    password: 1111
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  web:
    resources:
      static-locations: classpath:/templates, classpath:/static/css
#  freemarker:
#    expose-request-attributes: true